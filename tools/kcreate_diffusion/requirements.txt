# Pinned at the major-version level — the real version is
# whichever the host's pip resolves at install time. Editing
# these versions affects only the optional Tier-2+ image-gen path;
# the rest of the editor builds and runs without any of this.
fastapi>=0.110,<0.120
uvicorn[standard]>=0.27,<0.40
pydantic>=2.5,<3
torch>=2.1
diffusers>=0.27
transformers>=4.40
safetensors>=0.4
accelerate>=0.27
Pillow>=10.0
